language: python
python:
  - "2.7"
before_install: "mv secret.testing.py secret.py"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests